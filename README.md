# FlashLingo

FlashLingo is a local-first Flutter app for importing `.flashjp` decks and
studying them on Android with spaced repetition, deck analytics and offline
progress tracking.

## Development

- `flutter pub get`
- `flutter test`
- `flutter analyze`

## Android release

Create `android/key.properties` with your release keystore settings before
building a signed Play Store bundle.
